<UserControl x:Class="ArchiveAccessPointVisualizer.FilePathFieldUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ArchiveAccessPointVisualizer"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="300">
    <Grid DataContext="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=local:FilePathFieldUserControl}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="{Binding Path=TextBoxToLabelWidthRatio}" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Label Content="{Binding Path=Label}" Grid.Column="0"></Label>
        <TextBox Text="{Binding Path=Value}" IsReadOnly="True" Grid.Column="1"></TextBox>
        <Button Content="{Binding Path=ButtonLabel}" Click="{Binding Path=OnButtonClick}" Grid.Column="2"></Button>
    </Grid>
</UserControl>
